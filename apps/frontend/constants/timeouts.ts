const WAIT_TIMEOUT = {
  SHORT: 3000,
  LONG: 5000,
};

const ALERT_TIMEOUT = {
  SHORT: 15000,
  LONG: 30000,
};

const TOAST_TIMEOUT = {
  SHORT: 10000,
};

const DEBOUNCE_TIMEOUT = {
  SHORT: 800,
  EDITOR_SAVE: 1000,
};

const STATUS_TEXT_TIMEOUT = {
  SHORT: 10000,
};

const TOOL_TIP_TIMEOUT = {
  SHORT: 350,
};

const ASYNC_MINIMUM_DELAY = {
  SHORT: 300,
};

export {
  WAIT_TIMEOUT,
  ALERT_TIMEOUT,
  TOAST_TIMEOUT,
  DEBOUNCE_TIMEOUT,
  STATUS_TEXT_TIMEOUT,
  TOOL_TIP_TIMEOUT,
  ASYNC_MINIMUM_DELAY,
};
